import React from 'react';
import { Menu } from 'semantic-ui-react';

const MenuComponent = ({ count, totalPrice, items }) => (
    <Menu>
        <Menu.Item name='browse' onClick={this.handleItemClick}>
            Diary PRJ
        </Menu.Item>
    </Menu>
);

export default MenuComponent;